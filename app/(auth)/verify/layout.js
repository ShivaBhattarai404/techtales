export const metadata = {
    title: "Verify Email"
}

const layout = ({children}) => {
  return (
    <section style={{margin: "12em auto"}}>
        {children}
    </section>
  )
}

export default layout